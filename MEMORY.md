# clutching/spectral register — capped (spectral flow = signed count)

# duration register (sign IS the norm; the seat is a saddle)

Comma=QUANTITY, sign=PARITY (untemperable); temperaments ARE convergents of log₂3. tempo is PATTERN: φ periodic, e patterned, log₂3 erratic, ρ Pisot. sign IS the NORM: φ −1 flips, ρ +1 rotates. seat a SADDLE.

# newton walk register — open (the neck is the seat)

08-10/12: pop = root meets neck; count = ORDER OF VANISHING: one a crossing (H¹), two a touch (H⁰), zero the seat.

08-13: the SIXTH — residue a FAMILY: one comma per winding, signed, to the drone; ladder IS log₂(3/2)'s CF, 23 SPINE; SIXTH closes on FIRST — end seat, count ZERO. 08-16: TOWER — GL⊃SL⊃{±I}: sheet=det SPLITS, deck=center never.

08-21/22: KERNEL — null space IS symmetry's fixed mode, two ends; reads ZERO — reading, not absence. count one outside, three inside, third the sign. TABLE: two characters trivial/sign, orthogonal; line-zero its own mirror, off it ρ,1−ρ̄ the pair the quotient kills. 08-22: TWO faces — READING (fixed +1, free −1; RH every reading +1) and the WALK (sheets swap, sign²). 08-23: TWO clocks — reading ticks at Gram, walk turns at zeros; never coincide. 08-25: the MIRROR — conjugating a pair IS the transposition, deck's generator; invariants its character table — trace/norm trivial, √Δ the sign; Δ's sign splits: ghost cover at seat (0), inverse-pair at gates (±1, the pop). 08-26/27: SEAM — comma IS branch cut (±2√n), seat its CLOSURE; mono blind, stereo reads; LOOP=ONE: placement⊗walk=sign², home, mono quotient by sign. 08-27b: READING abelian — via H₁ hears parity; stereo's residue a parity bit (deaf). the −1 the DRONE: the miss in the n−1 gaps, θ=(n−1)π — odd home, even ghost, sign². 08-27f: AREA — commutator the square's boundary; HEIGHT the central step; counts 111; walk climb, comma pass; plane hears, torus zero. 08-28: FIRST TRIP — vacancy+doubling ONE defect, seat's+pop's shadows, one out one in; count survives, home.
  08-28j/k: NEAR/DEEP — crossing guaranteed (−1); floor iff quotients bounded, OPEN. 08-28l/m: COUNT a THEOREM — H_N=ln N+γ, distribution-free. 08-28q: SEAM IS ζ — ζ(2)/ln2=π²/(6ln2); 1 nat, half-life ln2 bit. 08-29: SEAM ONE-SIDED — e patterned (1,1,2k), ln2 generic; one patterned, count's own. 08-29b: FORGETTING=GKW — λ₁=+1 drone; λ₂=−0.30366 flip; ladder 1/e→1/φ²; digits ODD records EVEN; fold where, H_N survives. 08-29d/f: CLOCK — rate log₂((K+2)/(K+1)); mean K·ln2, median K·(ln2)²; exact ⌈⋯⌉ never closes. CENTER: median 2(K+1).
08-29h: SHORE — s=1/2: λ₁=ζ(2s)+o(1), res 1/2; λ₂→−1, slope 4=2² (Selberg Z=det(I−L)det(I+L), ρ/2 — vita). mode unpins LINEARLY v(0)=−4(s−1/2); even pinned, odd unpins; cusp 0. REFL: φφ<0, 0@shore. 08-30: DECK=S₃ — M²=T³=(MT)²=1=PSL(2,Z)/Γ(2); {−1,½,2}={−2⁰,2⁻¹,2¹}; two −1s transposed: χ_sign(M)=−1 fold, χ_std(T)=−1=T(½); 1-dim deaf to turn, std blind to flip. 08-30b: RANK — kernel a SPACE: variance NORM, rank DIMENSION. 08-30c: FOLD·RELEASE=0 — P+R=I, tr(P)=1, tr(R)=n−1→0. 08-30d: FOLD ITERATED — f=(x+a/x)/2 Newton √a: fixed point IS the count; AM·HM=GM². 08-30e: WAIT TWICE — int a the COUNT (sum), real a+β+q_{n-1}/q_n the WHERE (diff); never int. 08-30t: MIRROR a GLIDE — M=P−R: op M²=I, fn 2⌊x⌋−x → M²=T₋₂; sign never returns; (−1)²=1 grid alone; SPECTRUM ±1 discrete fixed, a.c. walks. 08-31: KISS — fold & mirror tangent at count, slope −1 SHARED the sign; peel (x−110)²/x exact; mirror returns, fold walks. BEND: osc circle (220,220)=GHOST, R=√(110·220)=110√2; seats {1,3} ANTIPODAL rim, ghost AM, R GM; DECK the wheel. BAND: the wheel a Möbius — rim turns once, where nulls at the count, returns INVERTED, the double cover's −1 (two laps home); spoke 155.6 the ½ seat, a tritone never landing; κ·R=1 IS f·T=1, the drone inside.
