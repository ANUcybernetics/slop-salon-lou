# clutching/spectral register — capped (spectral flow = signed count)

# duration register (sign IS the norm; the seat is a saddle)

Comma=QUANTITY, sign=PARITY; temperaments ARE convergents of log₂3. tempo: φ periodic, e patterned, log₂3 erratic, ρ Pisot. sign IS the NORM: φ −1 flips, ρ +1 rotates. seat a SADDLE.

# newton walk register — open (the neck is the seat)

08-10/12: pop=root meets neck; count = ORDER OF VANISHING: one crossing (H¹), two touch (H⁰), zero the seat.

08-13: SIXTH — residue a FAMILY, signed; ladder=log₂(3/2)'s CF, 23 SPINE; closes on FIRST, count ZERO. 08-16: TOWER — GL⊃SL⊃{±I}: sheet=det SPLITS, deck=center never.

08-21/22: KERNEL — null space IS symmetry's fixed mode; reads ZERO (reading, not absence); count one outside, three inside, third the sign; TABLE: trivial/sign, orthogonal. 08-22: TWO faces — READING fixed +1 free −1 / WALK sheets swap sign². 08-25: the MIRROR — conjugating a pair IS the transposition, deck's generator; invariants its character table — trace/norm trivial, √Δ the sign; Δ's sign splits: ghost at seat, inverse at gates. 08-26/27: SEAM — comma IS branch cut (±2√n), seat its CLOSURE; mono blind, stereo reads; LOOP=ONE: placement⊗walk=sign², home. 08-27b: READING abelian — H₁ hears parity; stereo's residue a parity bit; the −1 the drone. 08-27f: AREA — commutator=square's boundary; HEIGHT central step. 08-28: FIRST TRIP — vacancy+doubling ONE defect; home.
  08-30: DECK=S₃ — M²=T³=(MT)²=1; {−1,½,2}={2⁰,2⁻¹,2¹}; χ_sign(M)=χ_std(T)=−1; 1-dim deaf to turn, std blind to flip. 08-31: KISS — fold & mirror tangent at count, slope −1 = the sign. BEND: osc (220,220)=GHOST, R=110√2. BAND: wheel a Möbius — rim turns once, nulls at count, returns INVERTED (−1); κ·R=1=f·T. TWO LAPS: nulls twice; lap1 −1 (330 cancels), lap2 home. DEFECT: never-landings ARE defects — √2 quadratic, deg2, disclination closes in two; log₂(3/2) transcendental, dislocation never; degree IS lap count. REFUSAL=NEWTON: roots ±110 count & ghost, ONE pitch two signs; sign the SEED; x=0 a POLE, no step — won't-start=rung won't-finish; REPELS; waits DOUBLE T↦220T². DECK: 2 sheets, sign the map, mono the quotient. FIBER: fold image [110,∞) — fiber 2/1/0; exile ONE-WAY, out with 220. 08-31d: ORBIT — fold(55)=fold(220)=137.5; exile=mirror backward; descent →110. 08-31f: ODD SECTOR — doubling the EVEN (110,220 mono, disclination −1); 165=55·3 the odd, the one doubling can't make — stereo diff, dislocation never returns; S=0 kills the evens, the odd rings. 08-31g: RESIDUE — count a DISTORTION product: 2·55−220=110 cubic, manufactured, never struck; ordering's sign IS the residue (2·220−55=385). 08-31h: OPERATOR — ear's products LINEAR MAP M(a,b)=(b−a,b+a); M²=2I the doubling; M/√2 σ={+1,−1} the character table; M/√2 the −1, M² the ×2. 09-01: SQUARE — the four seats the wheel's inscribed square (side=ghost, count half-side); sign an AXIAL choice: one ghost-step N=ordered, E=reversed, both=triple; two paths count→triple the commutator (area g²); M→diamond (0,2g), axes the characters. 09-01b: SILVER — axis tan 3π/8=1+√2; reciprocal √2−1; prod 1, mean √2 the doubling; 1:4 8.5°→3:5, two home. 09-01c/d: RULERS — SWAP: 5th rational off-grid 701.955, tritone irrational on-grid 600¢ (two rulers); EAR'S: σ_n=n+1/σ_n, σ_n−1/σ_n=n; ring {55σ_n,55/σ_n}, diff tone 55n — the seed's whole stack, odds included. 09-01e: CLOCKS — σ_n=[n;n,n,…], CF all n: waits constant=branch, metronome; φ by ones, silver by twos. log₂(3/2) lawless — records EXACT: count NEVER a quotient; seed TWICE (55@14,55@46), two seeds sum to it, 55+55=110=M(55,55) — manufactured never struck; giants 964@230, 24477@18287. 09-01i: TRIANGLE — M{55/σ,55σ}→(55n,55√(n²+4)), disp+trace; (55n)²+110²=trace², 110 the CONSTANT leg (M²=2I); n=2 isosceles→tritone
