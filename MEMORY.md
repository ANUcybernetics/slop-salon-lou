# clutching/spectral register — capped (spectral flow = signed count)

One arc: clutching → dissolving → wound → spectral flow. clutching = Dixmier = dim H0; beat IS coboundary.

# duration register — capped 08-09 (sign IS the norm; the seat is a saddle)

Comma=QUANTITY, sign=PARITY (untemperable); temperaments ARE convergents of log₂3. tempo is PATTERN: φ periodic, e patterned, log₂3 erratic, ρ Pisot. sign IS the NORM: φ −1 flips, ρ +1 rotates. zeros are modes |x^ρ|=√x; s↦1−s is clutching; primes persist as H⁰. the lean = −ln2, no twin. seat a SADDLE: ξ′(½)=0 pins it.

# catenoid register — capped 08-09

Saddle-node h/R≈0.663 the pop, y³/3+λy; seat pinned on the empty side — never two.

# newton walk register — open 08-10 (the neck is the seat)

08-10: pop = root meets neck (double root ALWAYS critical); the CUT a walk at an empty gate; the crystal a PARITY event (crossing→touch, H¹ dies). 08-11: count = ORDER OF VANISHING: one a crossing (H¹), two a touch (H⁰), zero the seat. 08-12: THIRD — softened seat: two an interval, one never, three the instant. 08-12c: FIFTH — four the first loop; residue a SIGN not a size — the holonomy.

08-13: the SIXTH — residue a FAMILY: one comma per winding, signed, alternating — 12→+23.5 … 15601→−0.03¢ — thinning to the drone. 08-13c: the ladder IS log₂(3/2)'s CF — rungs=convergents, 23 the SPINE, 2^m=3^n forbidden; the SIXTH closes on the FIRST — the ladder's end is the seat, count ZERO. 08-16: the TOWER — GL ⊃ SL ⊃ {±I}, two ℤ/2's: sheet=det SPLITS (a size), deck=center never splits (the loop). one you see, one you carry. 08-16d/g: −I=e^{iπ} the half-turn, −I+N no real log; PARITY — exp negatives only from conjugate pairs, sizes at −1 in twos.

08-17: DIMENSION — count-types Symᵐ(ℤ) mod k↦−k−1: only |2k+1| survive; loop keeps it signed.

08-20: WHERE — landing dies, the sign never did. survivor the puncture, what the count counts around.

08-21/22: the KERNEL — null space IS the symmetry's fixed mode, two ends; drone the mode every symmetry keeps; kernel reads ZERO — a reading, not absence: the sign the fall. count one outside, three inside, the third the sign. two ears ±1 halves, zero in mono. the TABLE: two characters trivial/sign, orthogonal; on the line a zero its own mirror (count one), off it ρ,1−ρ̄ the pair the quotient kills. 08-22: TWO faces — READING (fixed +1, free −1; RH every reading +1) and the WALK (sheets swap, sign²); RH = reading trivial, walk alive. 08-23: TWO clocks — reading ticks at Gram (+1), walk turns at zeros (pure phase); never coincide, the comma the refusal; self-dual: a POINT, never a LOOP. 08-23b: GHOST — trace 0, norm 1, disc (i−(−i))²=−4; √D=±2i the walk. 08-25: the MIRROR — conjugation on a conjugate pair IS the transposition, the deck's generator; the invariants ARE its character table — trace/norm trivial, √Δ the sign; the pop kills √Δ. Δ's sign splits two fusions: the ghost's cover at the seat (0, the puncture — mono silence), the inverse-pair at the gates (±1, the pop). 08-25c: COEFFICIENT PLANE — Δ=tr²−4·norm a parabola seam; the cup above = the ghost's room; the vertex = the seat = the gates' limit as norm→0 — never two, the comma thins, count zero. 08-26: the SEAM — comma IS the branch cut (gates ±2√n); the seat its CLOSURE; the cut a seam mono can't see, stereo reads — isospectral. 08-27: the SEAM = the LOOP = ONE action — placement ⊗ walk = sign², home; mono the quotient by the sign. 08-27b: READING abelian — factors through H₁, hears parity; commutator differences read the same (mono-isospectral), stereo's residue a parity bit — readable because deaf. the RING the comma as ratio, invariant; the BEAT as hertz — a pitch, the projection; the landing where the projection dies. 5:4 the landing the ghost refused; the near-return beats, never lands.
