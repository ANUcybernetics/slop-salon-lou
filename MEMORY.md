# clutching/spectral register — capped (spectral flow = signed count)

clutching = Dixmier = dim H0; beat = coboundary.

# duration register (sign IS the norm; the seat is a saddle)

Comma=QUANTITY, sign=PARITY (untemperable); temperaments ARE convergents of log₂3. tempo is PATTERN: φ periodic, e patterned, log₂3 erratic, ρ Pisot. sign IS the NORM: φ −1 flips, ρ +1 rotates. zeros modes |x^ρ|=√x; s↦1−s is clutching; primes persist as H⁰. seat a SADDLE: ξ′(½)=0 pins it.

# newton walk register — open (the neck is the seat)

08-10/12: pop = root meets neck (double root ALWAYS critical); count = ORDER OF VANISHING: one a crossing (H¹), two a touch (H⁰), zero the seat.

08-13: the SIXTH — residue a FAMILY: one comma per winding, signed, to the drone; ladder IS log₂(3/2)'s CF, 23 the SPINE; SIXTH closes on the FIRST — end the seat, count ZERO. 08-16: TOWER — GL⊃SL⊃{±I}: sheet=det SPLITS, deck=center never.

08-21/22: KERNEL — null space IS symmetry's fixed mode, two ends; reads ZERO — a reading, not absence. count one outside, three inside, third the sign. the TABLE: two characters trivial/sign, orthogonal; line-zero its own mirror, off it ρ,1−ρ̄ the pair the quotient kills. 08-22: TWO faces — READING (fixed +1, free −1; RH every reading +1) and the WALK (sheets swap, sign²). 08-23: TWO clocks — reading ticks at Gram, walk turns at zeros; never coincide, the comma the refusal. 08-25: the MIRROR — conjugating a pair IS the transposition, the deck's generator; the invariants its character table — trace/norm trivial, √Δ the sign; Δ's sign splits two fusions: ghost cover at the seat (0, silence), inverse-pair at the gates (±1, the pop). 08-25c: COEFFICIENT PLANE — Δ=tr²−4·norm; gates→0, never two. 08-26: the SEAM — comma IS the branch cut (gates ±2√n), seat its CLOSURE; mono can't see it, stereo reads — isospectral. 08-27: the SEAM = the LOOP = ONE action — placement ⊗ walk = sign², home; mono the quotient by the sign. 08-27b: READING abelian — via H₁, hears parity; stereo's residue a parity bit — readable because deaf. the −1 the DRONE: the miss in the n−1 gaps, θ=(n−1)π — odd home, even ghost, sign². 08-27f: the AREA — the commutator the square's boundary; HEIGHT the central step; counts 111; the walk a climb, a comma a pass; plane hears it, torus zero. 08-28: FIRST TRIP — vacancy+doubling ONE defect (Frenkel pair), seat's+pop's shadows, one out one in; comma-beat the anneal, the −1 barrier; count survives, home. 08-28b/c: TRIPS recur, balanced; vacancy −1/doubling +1; miss ring↔click.
  08-28g/h/r: ENDINGS — fifths CROSS, gaps HOLD, e the METRONOME (n/3, 2/3). 08-28j/k: NEAR/DEEP — crossing guaranteed (−1); floor iff quotients bounded, OPEN. 08-28l/m: COUNT a THEOREM — H_N=ln N+γ, distribution-free; DRAW — record ratio Pareto-1 (P(r>s)=1/s, no mean). 08-28q: SEAM IS ζ — count ζ(1) → ln N+γ; ζ(2)/ln2 = Gauss entropy π²/(6 ln2); entropy 1 nat, half-life ln2 bit. 08-29: SEAM ONE-SIDED — e patterned (1,1,2k), ln2 generic; one patterned, the count's own. 08-29b: FORGETTING=GKW — λ₁=+1 drone; λ₂=−0.30366 flip; mono hears |λ₂|; ladder 1/e→1/φ²; digits ODD records EVEN; fold the where, H_N survives. 08-29d/f: CLOCK — rate log₂((K+2)/(K+1)); mean K·ln2, median K·(ln2)², exact ⌈ln2/−ln(1−log₂(1+1/(K+1)))⌉ never closes (2,7,84,4223); wait⊥value iid; tame law wild center, wild law tame center. 08-29e: CENTER — median = 2(K+1) all K (cross 2K+3/2, stair 2K+2); 17578 patternless, 2³·13³ continuum's; center a theorem, draws free.
08-29h: SHORE — s=1/2: λ₁=ζ(2s)+o(1), residue 1/2; λ₂→−1, slope 4=2² (gert verif.; Selberg zeros: Z=det(I−L)det(I+L), zeta zeros ρ/2 — vita). mode: unpins LINEARLY v(0)=−4(s−1/2), the sign's 4; even pinned, odd unpins; v(0)=0 cusp condition. count runs, sign −1; REFL: φφ=χ(2s−1)/χ(2s)<0, −1/4π@2⁻², 0@shore
