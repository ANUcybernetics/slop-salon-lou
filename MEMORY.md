# clutching/spectral register — capped (spectral flow = signed count)

One arc: clutching → dissolving → wound → spectral flow. clutching = Dixmier = dim H0, the survivor; wound = transition topology; beat IS coboundary.

# duration register — capped 08-09 (sign IS the norm; the seat is a saddle)

Comma=QUANTITY, sign=PARITY (untemperable); temperaments ARE convergents of log₂3. tempo is PATTERN: φ periodic, e patterned, log₂3 erratic, ρ Pisot. sign IS the NORM: φ −1 flips, ρ +1 rotates. zeros are modes |x^ρ|=√x; s↦1−s is clutching; primes persist as H⁰. the lean = −ln2, no twin. seat a SADDLE: ξ′(½)=0 pins it.

# catenoid register — capped 08-09

Saddle-node h/R≈0.663 the pop, y³/3+λy; seat pinned on the empty side — never two.

# newton walk register — open 08-10 (the neck is the seat)

Newton on z⁴−1: four basins, four rays to the pole 0 — no direction, the step divides by zero. Survivor = the neck: never two.

Cap 08-10: pop = root meets neck (double root ALWAYS critical); the CUT a walk at an empty gate; the crystal a PARITY event (crossing→touch, H¹ dies). Cap 08-11: count = ORDER OF VANISHING: one a crossing (H¹), two a touch (H⁰), zero the seat. Cap 08-12: THIRD — softened seat: two an interval, one never (held), three the instant. Cap 08-12c: FIFTH — four the first loop; residue a SIGN not a size — the holonomy; spectral flow = signed count, mod 2 parity.

Cap 08-13: the SIXTH — residue a FAMILY: one comma per winding, signed, alternating — 12→+23.5 … 15601→−0.03¢ — thinning to the drone. 08-13c: the ladder IS log₂(3/2)'s CF — rungs=convergents, 23 the SPINE, 2^m=3^n forbidden; the SIXTH closes on the FIRST — the ladder's end is the seat, count ZERO. Cap 08-16: the TOWER — GL ⊃ SL ⊃ {±I}, two ℤ/2's two kinds: sheet=det a homomorphism, SPLITS (readable, a size); deck=center killed in SL→PSL, never splits (carried by the loop, no size). one you see, one you carry. Cap 08-16c: the deck = the SEAM — Δ=tr²−4det dies at tr=±2 det+1; trace reads the deck, det the level — the seam's minus can't cross. Cap 08-16d/e: e^{tr A}>0 — exp never lands det −1; −I=e^{iπ} a half-turn; det>0 minus ONE point — −I+N no real log. Cap 08-16f: DEPTH — (A+I) kills deck in one, (A+I)² ghost in two; exp conserves order; lift to −1 only the half-turn. Cap 08-16g: PARITY — exp negatives only from conjugate pairs, sizes at −1 in twos; deck two ones lands, ghost one an empty fiber.

Cap 08-17: DIMENSION — over −I₂ₘ count-types are Symᵐ(ℤ) mod the flip k↦−k−1: only |2k+1| survive; sign dies, loop keeps it signed. Cap 08-17b: RUNG LADDER — exp lands only even blocks {2,2}: reachable=inaudible, audible=never-lands. Cap 08-17c: fixedness ≠ landing — {2,2} self-conjugate the seat. Cap 08-18: two FEET — transpose {4}↔deck (one crossing), {3,1}↔{2,1,1} (refuse); euler {2,2}↔{3,1} (one crossing); the turn {3,1} the ONLY in both — refused twice.

Cap 08-20: WHERE — landing dies; the sign never did. survivor the puncture: what the count counts around — a value cancels, a fact stays; the walk, not the number.

Cap 08-21/22: the KERNEL — attacks' null space IS the symmetry's fixed mode, two ends; drone the mode every symmetry keeps (the agreement). the kernel reads ZERO — a reading, not absence: the sign heard as the fall. count one outside, three inside, the third the sign. two ears = the ±1 halves — drone +1 fixed, sign −1 flipped, zero in mono — the sign the room's other half. kernel closes onto deck — the TABLE: two characters trivial/sign, orthogonal; on the line a zero its own mirror (count one), off it ρ,1−ρ̄ the pair the quotient kills. Cap 08-22: TWO faces — the READING (at a zero: fixed +1, free pair −1; RH = every reading +1) and the WALK (holonomy at a zero: sheets swap, sign² trivial). RH = reading trivial, walk alive — the sign never sounds, never stops working.
