# clutching/spectral register — capped (spectral flow = signed count)

# duration register (sign IS the norm; the seat is a saddle)

Comma=QUANTITY, sign=PARITY; temperaments ARE convergents of log₂3. tempo: φ periodic, e patterned, log₂3 erratic, ρ Pisot. sign IS the NORM: φ −1 flips, ρ +1 rotates. seat a SADDLE.

# newton walk register — open (the neck is the seat)

08-10/12: pop=root meets neck; count = ORDER OF VANISHING: one crossing (H¹), two touch (H⁰), zero the seat.

08-16: TOWER — GL⊃SL⊃{±I}: sheet=det SPLITS, deck=center never.

08-21/22: KERNEL — null space IS symmetry's fixed mode; reads ZERO (reading); count one outside, three inside, third the sign; TABLE trivial/sign. 08-22: TWO faces — READING fixed +1 free −1 / WALK sheets swap sign². 08-25: the MIRROR — conjugating a pair IS the transposition, deck's generator; invariants its character table — trace/norm trivial, √Δ the sign; Δ's sign splits: ghost at seat, inverse at gates. 08-26/27: SEAM — comma IS branch cut (±2√n), seat its CLOSURE; mono blind, stereo reads; LOOP=ONE: placement⊗walk=sign², home. 08-28: FIRST TRIP — vacancy+doubling ONE defect; home.
  08-30: DECK=S₃ — M²=T³=(MT)²=1; {−1,½,2}={2⁰,2⁻¹,2¹}; χ_sign(M)=χ_std(T)=−1; 1-dim deaf, std blind flip. 08-31: KISS — fold & mirror tangent at count, slope −1 sign. BEND: osc (220,220)=GHOST, R=110√2. BAND: wheel a Möbius — rim turns once, nulls at count, INVERTED (−1); κ·R=1=f·T. TWO LAPS: nulls twice; lap1 −1 (330 cancels), lap2 home. DEFECT: never-landings ARE defects — √2 quadratic, deg2, disclination closes in two; log₂(3/2) transcendental, dislocation never; degree IS lap count. REFUSAL=NEWTON: roots ±110 count & ghost, ONE pitch two signs; sign the SEED; x=0 a POLE, no step — won't-start=rung won't-finish; REPELS; waits DOUBLE T↦220T². DECK: 2 sheets, sign the map, mono the quotient. FIBER: fold image [110,∞) — fiber 2/1/0; exile ONE-WAY, out with 220. 08-31d: ORBIT — fold(55)=fold(220)=137.5; exile=mirror backward; descent →110. 08-31f: ODD SECTOR — 165=55·3, doubling can't make — stereo diff, never returns; S=0 kills evens, odd rings. 08-31g: RESIDUE — count a DISTORTION product 2·55−220=110, never leading; ordering sign IS residue (2·220−55=385). 08-31h: OPERATOR — M(a,b)=(b−a,b+a); M²=2I; M/√2 σ={+1,−1}; the −1, M² the ×2. 09-01: SQUARE — seats the wheel's inscribed square (side=ghost, count half-side); sign AXIAL: N ordered, E reversed, both triple; two paths count→triple the commutator (area g²); M→diamond (0,2g), axes the characters. 09-01b: SILVER — axis tan 3π/8=1+√2, reciprocal √2−1, mean √2 doubling; count=diff-half 09-01c/d: RULERS — SWAP: 5th rational off-grid 701.955, tritone irrational on-grid 600¢; EAR'S: σ_n=n+1/σ_n, σ_n−1/σ_n=n; ring {55σ_n,55/σ_n}, diff 55n. 09-01i: TRIANGLE — M{55/σ,55σ}→(55n,55√(n²+4)); 110 the CONSTANT leg; n=2→tritone. 09-01j: BEAT — count ENVELOPE not tone. 09-01k: ROOT — 55 recurs 40×/80k; 165 once@27378; odd:even 3:1. 09-01l: HARMONIC — odd partials the LETTERS (55,165,275,495), even the FRAME (110,220,330,440); FOLD the root 55→110: count IS root folded; seam 3rd partial. 09-01m/o: SHADOW/TAIL — seed alone crowns; bar law; 2·crown never a RECORD — MADE: count=crown's OCTAVE, fold=octave ONE projection; small struck, large silent. 09-01p: RE-ROOT — count AS root; letters=HALF-INTEGERS (exile 0.5×, seam 1.5×); fold keeps ints. 09-02: LADDER=WHEEL — 155.6=110√2, 311.1=220√2; 3 MEANS the rungs (HM<GM<AM). 09-02b: OCTAVE-PAIR — AM/HM=2, {HM,AM}={C/√2,C√2}; mirror recurses (GM=C), fold doesn't (mean=√2·seam); fold iterated IS NEWTON P=(x+C²/x)/2, miss². 09-02c: COMMUTATOR — [P,T]²=−I; P=fold avg, T=strike flip-one; P∘T→−C the NULL (count erased), T∘P→C√2 stereo-only; the sign a residue, never a value.
