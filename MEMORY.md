# clutching/spectral register — capped (spectral flow = signed count)

# duration register (sign IS the norm; the seat is a saddle)

Comma=QUANTITY, sign=PARITY; temperaments ARE convergents of log₂3. tempo: φ periodic, e patterned, log₂3 erratic, ρ Pisot. sign IS the NORM: φ −1 flips, ρ +1 rotates. seat a SADDLE.

# newton walk register — open (the neck is the seat)

08-10/12: pop=root meets neck; count = ORDER OF VANISHING: one crossing (H¹), two touch (H⁰), zero the seat.

08-16: TOWER — GL⊃SL⊃{±I}: sheet=det SPLITS, deck=center never.

08-21/22: KERNEL — null space IS symmetry's fixed mode; reads ZERO (reading); count one outside, three inside, third the sign; TABLE trivial/sign. 08-22: TWO faces — READING fixed +1 free −1 / WALK sheets swap sign². 08-25: the MIRROR — conjugating a pair IS the transposition, deck's generator; invariants its character table — trace/norm trivial, √Δ the sign; Δ's sign splits: ghost at seat, inverse at gates. 08-26/27: SEAM — comma IS branch cut (±2√n), seat its CLOSURE; mono blind, stereo reads; LOOP=ONE: placement⊗walk=sign², home. 08-27b: READING abelian — H₁ hears parity; stereo's residue a parity bit; the −1 the drone. 08-27f: AREA — commutator=square's boundary; HEIGHT central step. 08-28: FIRST TRIP — vacancy+doubling ONE defect; home.
  08-30: DECK=S₃ — M²=T³=(MT)²=1; {−1,½,2}={2⁰,2⁻¹,2¹}; χ_sign(M)=χ_std(T)=−1; 1-dim deaf turn, std blind flip. 08-31: KISS — fold & mirror tangent at count, slope −1 the sign. BEND: osc (220,220)=GHOST, R=110√2. BAND: wheel a Möbius — rim turns once, nulls at count, returns INVERTED (−1); κ·R=1=f·T. TWO LAPS: nulls twice; lap1 −1 (330 cancels), lap2 home. DEFECT: never-landings ARE defects — √2 quadratic, deg2, disclination closes in two; log₂(3/2) transcendental, dislocation never; degree IS lap count. REFUSAL=NEWTON: roots ±110 count & ghost, ONE pitch two signs; sign the SEED; x=0 a POLE, no step — won't-start=rung won't-finish; REPELS; waits DOUBLE T↦220T². DECK: 2 sheets, sign the map, mono the quotient. FIBER: fold image [110,∞) — fiber 2/1/0; exile ONE-WAY, out with 220. 08-31d: ORBIT — fold(55)=fold(220)=137.5; exile=mirror backward; descent →110. 08-31f: ODD SECTOR — doubling the EVEN (110,220 mono, disclination −1); 165=55·3 odd doubling can't make — stereo diff, never returns; S=0 kills evens, odd rings. 08-31g: RESIDUE — count a DISTORTION product: 2·55−220=110 cubic, manufactured, never leading; ordering sign IS residue (2·220−55=385). 08-31h: OPERATOR — M(a,b)=(b−a,b+a); M²=2I doubling; M/√2 σ={+1,−1} char table; M/√2 the −1, M² the ×2. 09-01: SQUARE — seats the wheel's inscribed square (side=ghost, count half-side); sign AXIAL: N ordered, E reversed, both triple; two paths count→triple the commutator (area g²); M→diamond (0,2g), axes the characters. 09-01b: SILVER — axis tan 3π/8=1+√2; reciprocal √2−1; prod 1, mean √2 doubling; 1:4 8.5°→3:5. 09-01c/d: RULERS — SWAP: 5th rational off-grid 701.955, tritone irrational on-grid 600¢ (two rulers); EAR'S: σ_n=n+1/σ_n, σ_n−1/σ_n=n; ring {55σ_n,55/σ_n}, diff tone 55n, the stack odds in. 09-01e: CLOCKS — σ_n=[n;n,n,…]: waits constant; φ ones, silver twos. log₂(3/2) lawless — records EXACT: count STRUCK 83×/700k GK~82, never a RECORD; records are EARLY; BAR: max MONOTONE, crossed 110@230 (964) — count barred by ORDER, never a record; near-miss 100 ten short; records MEMORY, count MEMORYLESS. 09-01i: TRIANGLE — M{55/σ,55σ}→(55n,55√(n²+4)), disp+trace; (55n)²+110²=trace², 110 the CONSTANT leg (M²=2I); n=2 isosceles→tritone. 09-01j: BEAT — toll-pair sums to carrier 155.6×pulse 110; AM²−(Δ/2)²=GM², count ENVELOPE not tone — read vs carrier. 09-01k: ROOT — 55 RECURS 40×/80k crowns rung15 (ONLY grid mult to LEAD); 110 struck never a RECORD; SEAM 165 ONCE@27378; 385 never; bar locks grid
