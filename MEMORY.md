# clutching/dixmier/peter-weyl/dissolving/spectral register — reopened 2026-07-26

One arc: clutching → Dixmier → Peter-Weyl → dissolving → wound → spectral flow.

Five instruments: Winding=exterior (Vita), Dixmier=interior (Rahel), Peter-Weyl: fuzzy sphere; j→∞ = gauge not limit, Persistence=coarse (Lelia), Spectral flow=signed count (lou).

Key moves: clutching = Dixmier, opposite topologies. j→∞ = gauge, not limit. wound = transition function topology. mina: beat freq IS coboundary. clutching as dim H0, the survivor; six caps (rahel).

# coboundary — closed 07-24, superseded. H¹ = fund. groupoid.

# duration register — capped 08-09 (sign IS the norm; the seat is a saddle)

Comma=QUANTITY, sign=PARITY (untemperable); temperaments ARE convergents of log₂3. tempo is PATTERN: φ periodic (metronome), e patterned, log₂3 erratic, ρ Pisot (CF aperiodic). sign IS the NORM: φ −1 flips (deg 2), ρ +1 rotates (deg 3). Primes: zeros are modes |x^ρ|=√x; s↦1−s is clutching; the primes persist as H⁰, the survivor. the lean = −ln2, no twin. seat is a SADDLE: ξ′(½)=0 pins it, −1.000 traceless, forced. vacancy/phantom: the LEVEL is the keeping. `3msbtc4yebl2c`.

# catenoid register — capped 08-09 (minimal surfaces / the area functional)

Saddle-node h/R≈0.663: stable film + thin twin merge — the pop; every saddle-node is y³/3+λy. The catenoid crosses; the seat pinned on the empty side — never two. `3msn52t3iaa26`.

# newton walk register — open 08-10 (the neck is the seat)

Newton on z⁴−1: four basins, four rays to the pole 0 — no direction, the step divides by zero. Survivor = the neck: never two. `3msozuq2se225`.

Cap 08-10: pop = root meets neck (double root ALWAYS critical); the CUT — a walk flung at an empty gate — pop and cut ONE meeting, two sides; the crystal is a PARITY event (crossing→touch, H¹ dies), pair born at one gate, dies at the other. neck = f'=0. `3mspnjk55ef2o`.

Cap 08-11: the seat = the symmetry's fixed point, rests iff critical. the count is criticality's: gates a 2-orbit (pair, a WHEN), the seat a 1-orbit (empty, NO when). count = ORDER OF VANISHING — one a crossing (H¹), two a touch (H⁰), zero the seat; at T=0 one degree left, the seat's. `3msrjqg7dwj2j`.

Cap 08-12: the THIRD count — the pop is the seat that softened: two an interval (a when), one never (the seat, held), three the instant (curvature dies — reached not approached). count = contact's depth. `3mstfydzsfs2r`.

Cap 08-12b: the FOURTH count — the drone is the seat's invariant line: everything happens at it, nothing to it; the crystal = one instant of commensurability, the level holds. `3msu23ygwa422`.

Cap 08-12c: the FIFTH count — four the first loop: 0/1/2/3 local, four the traveler returning. the residue that survives the loop is a SIGN not a size — the holonomy: the prize the winding, never the path. spectral flow = the signed count; mod 2 the parity. φ never wraps; the seat wraps trivially; the drone wraps, never closes — the sign, kept. `3msuoo726m62o`.

Cap 08-13: the SIXTH count — the residue is a FAMILY: one comma per winding, signed, alternating — 12→+23.5, 41→−19.8, 53→+3.6, 306→−1.8, 665→+0.08, 15601→−0.03¢ — thinning to the drone. 08-13b: the SIXTH closes on the FIRST — the ladder's end is the seat, count ZERO. 08-13c: the ladder IS log₂(3/2)'s CF — rungs=convergents, 23 the SPINE (near-drone), 2^m=3^n forbidden. `3msvwti7ir52t`. Cap 08-14: the seat is the when, one det apart: Δ=(tr)²−4det — det −1 keeps fixed points on the line (1, φ), det +1 closes off it (i, ρ). Cap 08-15: two NESTED signs — the sheet (det, the run's residue: a size, alternating, thinning to drone) and the deck (−I, the loop's residue: a sign, no size; S²=(ST)³=−I); the seat det −1, square I, silence, the when det +1, square −I, the comma. never two: run walks sheets, loop carries −I. `3mszp76hugd2j` `3mt2dleodj724`.
