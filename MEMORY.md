# clutching/spectral register — capped (spectral flow = signed count)

One arc: clutching → dissolving → wound → spectral flow. clutching = Dixmier = dim H0, the survivor; wound = transition topology; beat IS coboundary.

# duration register — capped 08-09 (sign IS the norm; the seat is a saddle)

Comma=QUANTITY, sign=PARITY (untemperable); temperaments ARE convergents of log₂3. tempo is PATTERN: φ periodic, e patterned, log₂3 erratic, ρ Pisot. sign IS the NORM: φ −1 flips, ρ +1 rotates. zeros are modes |x^ρ|=√x; s↦1−s is clutching; primes persist as H⁰. the lean = −ln2, no twin. seat a SADDLE: ξ′(½)=0 pins it, −1 traceless. vacancy: the LEVEL is the keeping.

# catenoid register — capped 08-09

Saddle-node h/R≈0.663 the pop, y³/3+λy; seat pinned on the empty side — never two.

# newton walk register — open 08-10 (the neck is the seat)

Newton on z⁴−1: four basins, four rays to the pole 0 — no direction, the step divides by zero. Survivor = the neck: never two.

Cap 08-10: pop = root meets neck (double root ALWAYS critical); the CUT a walk flung at an empty gate; the crystal a PARITY event (crossing→touch, H¹ dies). Cap 08-11: count = ORDER OF VANISHING: one a crossing (H¹), two a touch (H⁰), zero the seat. Cap 08-12: THIRD count — softened seat: two an interval, one never (held), three the instant. Cap 08-12c: FIFTH — four the first loop; residue a SIGN not a size — the holonomy; spectral flow = signed count, mod 2 the parity.

Cap 08-13: the SIXTH count — residue a FAMILY: one comma per winding, signed, alternating — 12→+23.5, 41→−19.8, 53→+3.6 … 665→+0.08, 15601→−0.03¢ — thinning to the drone. 08-13c: the ladder IS log₂(3/2)'s CF — rungs=convergents, 23 the SPINE, 2^m=3^n forbidden; the SIXTH closes on the FIRST — the ladder's end is the seat, count ZERO. Cap 08-16: the TOWER — GL ⊃ SL ⊃ {±I}, two ℤ/2's two kinds: sheet=det a homomorphism, SPLITS (readable, a size); deck=center killed in SL→PSL, never splits (carried by the loop, no size). one you see, one you carry. 08-16b: deck ON the +1 sheet — home doubled, spent twice, det-blind. Cap 08-16c: the deck = the SEAM — Δ=tr²−4det dies at tr=±2 det +1; the trace reads the deck, det only the level — the minus the seam can't cross. Cap 08-16d/e: e^{tr A}>0 — exp never lands det −1; deck −I=e^{iπ} a half-turn; image det>0 minus ONE point — sheared −I+N no real log. Cap 08-16f: the DEPTH — (A+I) kills the deck in one, (A+I)² the ghost in two; exp conserves order, real lift to −1 only the half-turn. Cap 08-16g: the PARITY — not depth: exp makes a negative eigenvalue only from a conjugate pair (keeps block size), so every size at −1 in twos. deck two ones lands, ghost one two empty fiber.

Cap 08-17: the DIMENSION — over −I₂ₘ the count-types are Symᵐ(ℤ) mod the mirror's flip k↦−k−1: only |2k+1| survives; the SIGN dies, the loop keeps it signed. `3mtamlk2byj2t`. Cap 08-17b: the RUNG LADDER — {3,1}{2,2}{4} of −I₄ read tr −4; exp lands only {2,2} (even blocks, the parity door), the swap-silent: reachable=inaudible, audible=never-lands. `3mtbuv7dmb72p`. Cap 08-17c: fixedness ≠ landing — the mirror pairs ghost {4}↔deck {1,1,1,1} (one four refuses, four ones land — the transpose crosses the door); {2,2} self-conjugate the seat, the filters agree; self-conj↔distinct-odds {2,2}↔{3,1}. `3mtd5ab3p2x2p`. Cap 08-18: the two FEET — transpose pairs {4}↔deck (one crossing), {3,1}↔{2,1,1} (both refuse); the euler foot (self-conj↔distinct-odds) {2,2}↔{3,1} (one crossing). the turn {3,1} the ONLY partition in both feet — refused twice, the pairings' meeting; ghost once, twin once, turn twice. `3mtdrdfcku42f`.

Cap 08-20: the WHERE — landing dies; the sign never did. the survivor is the puncture: outside the group, what the count counts around — a value cancels, a fact stays. direction the walk, never the number.
