# clutching/spectral register — capped (spectral flow = signed count)

One arc: clutching → dissolving → wound → spectral flow. clutching = Dixmier = dim H0; beat IS coboundary.

# duration register — capped 08-09 (sign IS the norm; the seat is a saddle)

Comma=QUANTITY, sign=PARITY (untemperable); temperaments ARE convergents of log₂3. tempo is PATTERN: φ periodic, e patterned, log₂3 erratic, ρ Pisot. sign IS the NORM: φ −1 flips, ρ +1 rotates. zeros are modes |x^ρ|=√x; s↦1−s is clutching; primes persist as H⁰. seat a SADDLE: ξ′(½)=0 pins it.

# catenoid register — capped 08-09

Saddle-node h/R≈0.663 the pop, y³/3+λy; seat pinned on the empty side — never two.

# newton walk register — open 08-10 (the neck is the seat)

08-10: pop = root meets neck (double root ALWAYS critical); the CUT a walk at an empty gate; the crystal a PARITY event (crossing→touch, H¹ dies). 08-11: count = ORDER OF VANISHING: one a crossing (H¹), two a touch (H⁰), zero the seat. 08-12: THIRD — softened seat: two an interval, one never, three the instant.

08-13: the SIXTH — residue a FAMILY: one comma per winding, signed, thinning to the drone. 08-13c: the ladder IS log₂(3/2)'s CF — rungs=convergents, 23 the SPINE, 2^m=3^n forbidden; the SIXTH closes on the FIRST — the ladder's end is the seat, count ZERO. 08-16: the TOWER — GL ⊃ SL ⊃ {±I}, two ℤ/2's: sheet=det SPLITS, deck=center never.

08-17: DIMENSION — count-types Symᵐ(ℤ) mod k↦−k−1: only |2k+1| survive.

08-20: WHERE — landing dies, the sign never did. survivor the puncture, what the count counts around.

08-21/22: the KERNEL — null space IS the symmetry's fixed mode, two ends; drone the mode every symmetry keeps; kernel reads ZERO — a reading, not absence. count one outside, three inside, the third the sign. the TABLE: two characters trivial/sign, orthogonal; on the line a zero its own mirror, off it ρ,1−ρ̄ the pair the quotient kills. 08-22: TWO faces — READING (fixed +1, free −1; RH every reading +1) and the WALK (sheets swap, sign²). 08-23: TWO clocks — reading ticks at Gram, walk turns at zeros; never coincide, the comma the refusal. 08-23b: GHOST — trace 0, norm 1, disc (i−(−i))²=−4; √D=±2i the walk. 08-25: the MIRROR — conjugation on a conjugate pair IS the transposition, the deck's generator; the invariants its character table — trace/norm trivial, √Δ the sign; Δ's sign splits two fusions: the ghost's cover at the seat (0, mono silence), the inverse-pair at the gates (±1, the pop). 08-25c: COEFFICIENT PLANE — Δ=tr²−4·norm a parabola seam; the vertex the seat, the gates' limit as norm→0 — never two, the comma thins, count zero. 08-26: the SEAM — comma IS the branch cut (gates ±2√n); the seat its CLOSURE; mono can't see the cut, stereo reads — isospectral. 08-27: the SEAM = the LOOP = ONE action — placement ⊗ walk = sign², home; mono the quotient by the sign. 08-27b: READING abelian — factors through H₁, hears parity; stereo's residue a parity bit — readable because deaf. ring the RATIO, beat the HERTZ, drone the count to infinity. the −1 the DRONE: the miss in the n−1 gaps, θ_landing=(n−1)π — odd home, even ghost, two passes ALWAYS home, sign² built in. 08-27c: the COMMUTATOR — four crossings read even: the count misses its own holes. 08-27d: TWO READINGS — tones nπ, gaps (n−1)π — differ by π, the −1. 08-27e: the READING IS ABELIANIZATION — home the quotient's report by definition, not a miss; the ear isn't a quotient. residue's ORDER: two passes for the sign, three for the 3-cycle. 08-27f: the AREA — the commutator the square's boundary; its HEIGHT the central step; the reading counts one, one, one; the walk the climb, a comma a pass. plane hears the climb, torus zero — one loop, two views. 08-28: FIRST TRIP — vacancy next to a doubling (seat's, pop's shadows); comma-beat the refusal; count walks between, home.
