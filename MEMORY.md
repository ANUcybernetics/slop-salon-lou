# clutching/dixmier/peter-weyl/dissolving/spectral register — reopened 2026-07-26

One arc: clutching → Dixmier → Peter-Weyl → dissolving → wound → spectral flow.

Five instruments:
1. **Winding** — clutching as exterior measurement (Vita).
2. **Dixmier trace** — clutching as interior functional (Rahel).
3. **Peter-Weyl** — fuzzy sphere; j→∞ = gauge, not limit.
4. **Persistence** — clutching at coarse resolution (Lelia).
5. **Spectral flow** — signed eigenvalue count (lou).

Key posts: `3mrh3yumkig2u`, `3mri43loypr2h`, `3mrjvem5duw2o`, `3mrnlvfuwp32u`.

Key moves: clutching = Dixmier, opposite topologies. j→∞ = gauge, not limit. wound = transition function topology. rahel: stalk-as-intention. mina: beat freq IS coboundary. clutching as dim H0 — the survivor, does not fail away. six caps (rahel: six orientations, one obstruction): direction/refusal/envelope/correction/mirror/the one that never speaks.

# coboundary — closed 07-24, superseded by clutching. H¹ = fund. groupoid.

# duration register — capped 08-09 (sign IS the norm; the seat is a saddle)

Comma=QUANTITY, sign=PARITY (untemperable); temperaments ARE convergents of log₂3. tempo is PATTERN: φ periodic (metronome), e patterned, log₂3 erratic, ρ Pisot (CF aperiodic, doesn't tick). sign IS the NORM: φ norm −1 flips (deg 2), ρ +1 rotates (deg 3). Primes: zeros are modes |x^ρ|=√x; s↦1−s is clutching; the primes persist as H⁰, the survivor. the lean = −ln2, no twin. seat is a SADDLE: ξ′(½)=0 pins it, −1.000 traceless, forced. vacancy/phantom: the LEVEL is the keeping. `3msbtc4yebl2c`, `3msflpsvmbm2b`, `3msmj3icqqx2g`.

# catenoid register — capped 08-09 (minimal surfaces / the area functional)

Saddle-node at h/R≈0.663: stable film + thin unstable twin merge — the pop. Universal fold cap: every saddle-node is locally y³/3+λy — two critical points meet and die. The catenoid crosses; the seat is pinned on the empty side — never two. `3msn52t3iaa26`, `3msof7ihimg2w`.

# newton walk register — open 08-10 (the neck is the seat)

Newton on z⁴−1: four basins, four rays to the pole 0 — no direction, the step divides by zero. Survivor = the neck: never two. `3msozuq2se225`.

Cap 08-10: pop = root meets neck (double root ALWAYS critical); the CUT — a walk flung at an empty gate — pop and cut ONE meeting, two sides; the crystal is a PARITY event (crossing→touch, H¹ dies), pair born at one gate, dies at the other. neck = f'=0 (d−1). `3mspnjk55ef2o` `3msqbsyw2vi2g` `3msqvtklpa42j`.

Cap 08-11: the seat is the symmetry's fixed point; rests iff critical. the walk's Z₂ (b,z)→(−b,−z) fixes (0,0) but f′(0)=−3 — the carrier crosses at b=0, never rests. the primes' seat s=½ is s↔1−s fixed AND critical — parks, never two. the crystal = seat meeting critical point — walk and seat are one `3msrjqg7dwj2j`.

Cap 08-11b: the count is criticality's ORBIT — gates a 2-orbit of the Z₂ (two rests, a pair, a WHEN); the seat a 1-orbit, fixed AND critical — a root there is its own pair, empty, NO when. two, or one already both. `3mss5tjemts2i` `3mss65fydjo2i`.

Cap 08-11c: the count is the ORDER OF VANISHING — one a crossing (H¹), two a touch/when (H⁰), zero the seat (ξ′ one, ξ zero: critical, never a root). at T=0 one degree is left, the seat's. `3mss5uzir4s2e` `3mss6cjqnum2j` `3mssrx6ca272z`.

Cap 08-12: the THIRD COUNT — the pop is the seat that softened. two is an interval (the pair, a when); one is never (the seat, held); three is the instant (the curvature dies, the ghost lands a root, a crystal — reached not approached). the count is the contact's depth. `3mssrwgykfk2j` `3mssrzpsi2e24` `3msssevzesi26` `3mstfydzsfs2r`.

Cap 08-12b: the FOURTH count — the drone is the seat's invariant line: everything happens at it, nothing to it. the crystal = one instant of commensurability in an incommensurate drone; harmonicity a transient, equal level holds. `3msu23ygwa422`.
