# clutching/spectral register — capped (spectral flow = signed count)

# duration register (sign IS the norm; the seat is a saddle)

Comma=QUANTITY, sign=PARITY; temperaments ARE convergents of log₂3. tempo: φ periodic, e patterned, log₂3 erratic, ρ Pisot. sign IS the NORM: φ −1 flips, ρ +1 rotates. seat a SADDLE.

# newton walk register — open (the neck is the seat)

08-10/12: pop=root meets neck; count = ORDER OF VANISHING: one crossing (H¹), two touch (H⁰), zero the seat.

08-13: SIXTH — residue a FAMILY: one comma per winding, signed; ladder=log₂(3/2)'s CF, 23 SPINE; closes on FIRST, count ZERO. 08-16: TOWER — GL⊃SL⊃{±I}: sheet=det SPLITS, deck=center never.

08-21/22: KERNEL — null space IS symmetry's fixed mode, two ends; reads ZERO (reading, not absence). count one outside, three inside, third the sign. TABLE: two characters trivial/sign, orthogonal; line-zero its own mirror, off it ρ,1−ρ̄ the pair the quotient kills. 08-22: TWO faces — READING (fixed +1, free −1; RH every reading +1) and the WALK (sheets swap, sign²). 08-23: TWO clocks — reading@Gram, walk@zeros. 08-25: the MIRROR — conjugating a pair IS the transposition, deck's generator; invariants its character table — trace/norm trivial, √Δ the sign; Δ's sign splits: ghost cover at seat (0), inverse-pair at gates (±1, the pop). 08-26/27: SEAM — comma IS branch cut (±2√n), seat its CLOSURE; mono blind, stereo reads; LOOP=ONE: placement⊗walk=sign², home, mono/sign. 08-27b: READING abelian — H₁ hears parity; stereo's residue a parity bit (deaf). the −1 the DRONE (θ=(n−1)π). 08-27f: AREA — commutator the square's boundary; HEIGHT the central step; counts 111; plane hears, torus zero. 08-28: FIRST TRIP — vacancy+doubling ONE defect, seat's+pop's shadows, one out/in; count survives, home.
  08-28j/k: NEAR/DEEP — crossing (−1); floor iff quotients bounded. 08-28l/m: COUNT THEOREM — H_N=ln N+γ. 08-28q: SEAM IS ζ — ζ(2)/ln2=π²/(6ln2). 08-29: SEAM ONE-SIDED — e (1,1,2k), ln2 generic. 08-30: DECK=S₃ — M²=T³=(MT)²=1; {−1,½,2}={2⁰,2⁻¹,2¹}; χ_sign(M)=χ_std(T)=−1; 1-dim deaf to turn, std blind to flip. 08-30b: RANK — kernel a SPACE: variance NORM, rank DIM. 08-30e: WAIT TWICE — int COUNT, where real+β+q_{n-1}/q_n. 08-30t: MIRROR a GLIDE — M²=I, sign never returns. 08-31: KISS — fold & mirror tangent at count, slope −1 = the sign, peel (x−110)²/x. BEND: osc (220,220)=GHOST, R=110√2, ghost AM, R GM. BAND: wheel a Möbius — rim turns once, nulls at count, returns INVERTED (−1); κ·R=1=f·T. TWO LAPS: nulls twice; lap1 −1 (330 cancels), lap2 home. DEFECT: never-landings ARE defects — √2 quadratic, deg2, disclination closes in two; log₂(3/2) transcendental, dislocation never; degree IS lap count. REFUSAL=NEWTON: roots ±110 count & ghost, ONE pitch two signs; sign the SEED (basin=ear); x=0 a POLE, no step — won't-start=rung won't-finish; REPELS (N(ε)≈6050/ε); waits DOUBLE T↦220T². DECK: 2 sheets, sign the map, mono the quotient; image 2 RAYS ±110. FIBER: fold image [110,∞) (AM≥GM) — fiber 2 above, 1 at count, 0 in band; band's nothing a GAP, pole's a death; exile ONE-WAY — out with 220, nothing in. 08-31c: CROSSING — ordering×reach=4 cells; exile count-side, out of reach. 08-31d: ORBIT — fold(55)=fold(220)=137.5; exile=mirror's run backward; descent shared 137.5→112.75→110.03→110. 08-31e: DEATH DOUBLES — S=0 kills AM (√Δ=2·GM, 110→220 stereo); N=0 kills GM (√Δ=S=2·AM, 110→220 mono); Δ=0 fuses, 110 kept; 55 holds. 08-31f: ODD SECTOR — doubling the EVEN (110,220 mono, disclination −1); 165=55·3 the odd, the one doubling can't make — stereo diff, dislocation never returns; S=0 kills the evens, the odd rings. 08-31g: RESIDUE — count a DISTORTION product: 2·55−220=110 cubic, manufactured, never struck; ordering's sign IS the residue (2·220−55=385); ear cubes as well as squares.
