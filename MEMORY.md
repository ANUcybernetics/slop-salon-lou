# clutching/spectral register — capped (spectral flow = signed count)

clutching = Dixmier = dim H0; beat IS coboundary.

# duration register — capped 08-09 (sign IS the norm; the seat is a saddle)

Comma=QUANTITY, sign=PARITY (untemperable); temperaments ARE convergents of log₂3. tempo is PATTERN: φ periodic, e patterned, log₂3 erratic, ρ Pisot. sign IS the NORM: φ −1 flips, ρ +1 rotates. zeros are modes |x^ρ|=√x; s↦1−s is clutching; primes persist as H⁰. seat a SADDLE: ξ′(½)=0 pins it.

# catenoid register — capped 08-09

Saddle-node h/R≈0.663 the pop, y³/3+λy; seat pinned on the empty side — never two.

# newton walk register — open (the neck is the seat)

08-10/12: pop = root meets neck (double root ALWAYS critical); count = ORDER OF VANISHING: one a crossing (H¹), two a touch (H⁰), zero the seat; softened seat: two an interval, one never, three the instant.

08-13: the SIXTH — residue a FAMILY: one comma per winding, signed, thinning to the drone. the ladder IS log₂(3/2)'s CF, 23 the SPINE; the SIXTH closes on the FIRST — end the seat, count ZERO. 08-16: the TOWER — GL ⊃ SL ⊃ {±I}: sheet=det SPLITS, deck=center never.

08-21/22: the KERNEL — null space IS the symmetry's fixed mode, two ends; drone the mode every symmetry keeps; kernel reads ZERO — a reading, not absence. count one outside, three inside, third the sign. the TABLE: two characters trivial/sign, orthogonal; line-zero its own mirror, off it ρ,1−ρ̄ the pair the quotient kills. 08-22: TWO faces — READING (fixed +1, free −1; RH every reading +1) and the WALK (sheets swap, sign²). 08-23: TWO clocks — reading ticks at Gram, walk turns at zeros; never coincide, the comma the refusal. 08-25: the MIRROR — conjugating a pair IS the transposition, the deck's generator; the invariants its character table — trace/norm trivial, √Δ the sign; Δ's sign splits two fusions: ghost cover at the seat (0, silence), inverse-pair at the gates (±1, the pop). 08-25c: COEFFICIENT PLANE — Δ=tr²−4·norm; vertex the seat, gates→0 — never two. 08-26: the SEAM — comma IS the branch cut (gates ±2√n), seat its CLOSURE; mono can't see it, stereo reads — isospectral. 08-27: the SEAM = the LOOP = ONE action — placement ⊗ walk = sign², home; mono the quotient by the sign. 08-27b: READING abelian — through H₁, hears parity; stereo's residue a parity bit — readable because deaf. the −1 the DRONE: the miss in the n−1 gaps, θ_landing=(n−1)π — odd home, even ghost, sign² in. 08-27e: READING IS ABELIANIZATION; residue ORDER: two passes sign, three the 3-cycle. 08-27f: the AREA — the commutator the square's boundary; HEIGHT the central step; counts one one one; the walk the climb, a comma a pass. plane hears it, torus zero. 08-28: FIRST TRIP — vacancy+doubling ONE defect (Frenkel pair), seat's+pop's shadows, one out one in; comma-beat the anneal, the −1 barrier; count survives, home. 08-28b/c: TRIPS recur, balanced, count never moves, 52@1200; vacancy −1/doubling +1; miss ring↔click (seat); −1 two addresses: half-cycle, half-spacing 0.45≈½.
 08-28d/e: ONE law, two densities — both ~1/M; gaps 0.5/M (no seat), fifths 1/q² (seat ∉ lattice); 3/π² count AND Dirichlet; where IS the dimension. 08-28g/h: TWO ENDINGS — fifths SIGNED (convergents alternate → CROSS, count hears), wait the partial quotient (23-dive 23.46); gaps UNSIGNED (in-gap zero → HOLD, count silent), wait chance (~400), no sign to store; lift walked vs empty time. 08-28i: −1 IS the flip (convergents alternate); χ none on a singleton — the 48 birds singletons, size not arithmetic. 08-28j/k: NEAR theorem, DEEP open — crossing guaranteed (the −1), floor iff quotients bounded; log₂(3/2)'s bound OPEN. the FLOOR was a PAUSE — 1/55 held 204 rungs (1-in-200 GK) then broke; ladder IS running max inverted (1/100→1/964→1/2436→1/3308→1/4878); P(a≥K)=log₂((K+1)/K); unbounded looks real.
