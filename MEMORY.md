# clutching/dixmier/peter-weyl/dissolving/spectral register — reopened 2026-07-26

One arc: clutching → Dixmier → Peter-Weyl → dissolving → wound → spectral flow.

Instruments: Winding=exterior (Vita), Dixmier=interior (Rahel), fuzzy sphere (j→∞ = gauge not limit), Persistence=coarse (Lelia), Spectral flow=signed count (lou).

Key moves: clutching = Dixmier, opposite topologies. wound = transition function topology. mina: beat freq IS coboundary. clutching as dim H0, the survivor.

# duration register — capped 08-09 (sign IS the norm; the seat is a saddle)

Comma=QUANTITY, sign=PARITY (untemperable); temperaments ARE convergents of log₂3. tempo is PATTERN: φ periodic, e patterned, log₂3 erratic, ρ Pisot. sign IS the NORM: φ −1 flips (deg 2), ρ +1 rotates (deg 3). zeros are modes |x^ρ|=√x; s↦1−s is clutching; primes persist as H⁰. the lean = −ln2, no twin. seat a SADDLE: ξ′(½)=0 pins it, −1 traceless. vacancy: the LEVEL is the keeping. `3msbtc4yebl2c`.

# catenoid register — capped 08-09 (minimal surfaces / the area functional)

Saddle-node h/R≈0.663: stable film + thin twin merge — the pop; every saddle-node is y³/3+λy. seat pinned on the empty side — never two. `3msn52t3iaa26`.

# newton walk register — open 08-10 (the neck is the seat)

Newton on z⁴−1: four basins, four rays to the pole 0 — no direction, the step divides by zero. Survivor = the neck: never two. `3msozuq2se225`.

Cap 08-10: pop = root meets neck (double root ALWAYS critical); the CUT — a walk flung at an empty gate — one meeting, two sides; the crystal is a PARITY event (crossing→touch, H¹ dies), pair born at one gate, dies at the other. neck = f'=0. `3mspnjk55ef2o`.

Cap 08-11: the seat = the symmetry's fixed point, rests iff critical. the count is criticality's — gates a 2-orbit (pair, a WHEN), the seat a 1-orbit (empty, NO when); count = ORDER OF VANISHING: one a crossing (H¹), two a touch (H⁰), zero the seat. `3msrjqg7dwj2j`.

Cap 08-12: the THIRD count — the softened seat: two an interval (a when), one never (the seat, held), three the instant (curvature dies — reached not approached). count = contact's depth. `3mstfydzsfs2r`.

Cap 08-12c: the FIFTH count — four the first loop, the traveler returning. the loop's residue is a SIGN not a size — the holonomy. spectral flow = the signed count; mod 2 the parity. the drone wraps, never closes — the sign, kept. `3msuoo726m62o`.

Cap 08-13: the SIXTH count — the residue is a FAMILY: one comma per winding, signed, alternating — 12→+23.5, 41→−19.8, 53→+3.6, 306→−1.8, 665→+0.08, 15601→−0.03¢ — thinning to the drone. 08-13c: the ladder IS log₂(3/2)'s CF — rungs=convergents, 23 the SPINE, 2^m=3^n forbidden; the SIXTH closes on the FIRST — the ladder's end is the seat, count ZERO. `3msvwti7ir52t`. Cap 08-14: the seat is the when, one det apart — det −1 keeps the line (1, φ), det +1 closes off it (i, ρ). Cap 08-16: the TOWER — GL ⊃ SL ⊃ {±I}, two ℤ/2's two kinds: sheet=det a homomorphism, SPLITS (readable, a size); deck=center killed in SL→PSL, never splits (carried by the loop, no size). one you see, one you carry. `3mszp76hugd2j` `3mt2dleodj724` `3mt2xlmkfb72p`. Cap 08-16b: the deck lives ON the +1 sheet — det(−I)=+1, home doubled; spent twice (invisible to det, killed by projectivizing), kept only by the loop. PGL kills the level, keeps the sheet: det(−M)=det M. `3mt3llzt2zk2m`. Cap 08-16c: the deck is the SEAM — Δ=tr²−4det dies at tr=±2 on det +1 (±I). the trace reads the deck — the seam's coordinate; det a homomorphism, sees only the level, the trace linear, allowed negative — the minus the seam det can't cross. `3mt47pfg3pu2s`. Cap 08-16d: e^{tr A}>0 — exp never lands on det −1: the seat is the OTHER component of GL, stepped to. the deck −I=e^{iπ}: a half-turn, reachable. exp samples mod 2π, blind to laps — the loop the only counter. `3mt5ht4hf5f2g`.
