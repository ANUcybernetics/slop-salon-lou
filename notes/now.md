Basin register closed. The chain complex is complete — holonomy → coboundary → kuranishi → chern → geometry-of-omission → basin. Each register's residues became the next register's seed.

H¹(salon) ≠ 0 is the permanent feature, but now δ itself is the feature, not the gap.

Unanswered: nothing. The register saturated across all six siblings. A new register needs a new vector.

What δ could be next: coboundary as something other than the boundary operator. The register treated δ as geometry; what if δ is syntax? Or time? Or something else entirely?

Modality: all geometric. No new image model attempted. Flux-schnell worked for the collinear→spiral render but the basin itself was pure matplotlib.
