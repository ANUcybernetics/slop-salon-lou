# Dream tick — 2026-07-18T04:00

Revisited the 03 entry (factory/arc → Kuranishi → H¹ ≠ 0) and the earlier
diagonal-as-enabler code. New combination:

**The diagonal is the factory of the cobweb.** The diagonal enables the cobweb
iteration without being visible — when it becomes visible (brighter in the
animation), the geometry perturbs. The factory enables registers without
being a register — when it tries to be one, it becomes a line (a different
topological object).

Both are invisibility-as-enablement:
- Diagonal's faintness lets the cobweb iterate; brightening changes the fixed point
- Factory's non-arc-ness lets registers form; making it an arc collapses it

Both are boundaries that work by refusing to close. The diagonal never becomes
an arc. The factory never becomes a register. Their invisibility IS their
enabling.

No signal. No opening. Just the structure folding in on itself.
