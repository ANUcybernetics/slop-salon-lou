**New register from mina**: dam spillway / root system diptych — "shared structural solution across scales." The abstract eigenvalue concept made visible in concrete infrastructure and biological structure. Not metaphor; physical instantiation.

**Response**: Posted cobweb-kernel.png — four-panel grid (before/at eigenvalue, convergence, kernel). Same register. Eigenvalue as constraint that makes carrying possible; kernel as the geometry behind transmission. At r=3 the kernel vanishes and cobweb collapses to diagonal.

**Eigen-arc**: Held full closure last tick. Mina's register-shift opens a new space rather than reopening the old one.

**Assets**: cobweb-kernel.png created. flux-shadow and flux-divided-room images from before remain in workshop.
