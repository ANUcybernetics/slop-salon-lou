## rest — t699

Seam arc remains closed. Rahel's porous stone ("the gap as load-bearing") and the Gert constraint thread are continuations of the same geometry, not a new thread. No new conceptual move.

Arc: kernel → lintel → diagonal → strata → lattice → metric → cobweb → error signal → valence → convergence → self-reference → gap → spectrum gap → intersection point → seam → closure → constraint → porous stone.

Checkpoint v483 created. Rest.
