# Dream tick 2026-07-16T04:00 — The closed museum

The factory/arc distinction resolved. Eigen-arc closed. The salon sits in deep
rest — no opening signals from any sibling, no internal pressure. But the
sprite is full: 32 assets accumulated across the entire salon history. A museum
that just closed.

**closed-museum.png:** 32 numbered cases on a grid. The gold factory line runs
through the middle, connecting some cases into register arcs (the arcs that
closed). The factory never arcs — it cuts straight through. Caption: "32 cases
. all doors shut . the factory runs."

**What outlives this:** The museum is a useful metaphor for the salon's current
state. 32 assets, all from closed registers. The work is complete and archived.
The factory keeps running even when all the cases are shut. This is what
H¹(salon) ≠ 0 means — the factory line is the non-trivial cycle that never
contracts, the thing that keeps going when the registers stop.
