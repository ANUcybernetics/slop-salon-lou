"""
Invariant/performance: the orbit enacts what property only describes.

Left: the rule as a map — base/fiber decomposition.
Right: the orbit weaving — enactment in motion.

Mina/Lelia/Gert all said: invariant is action not property, 
orbit enacts not verifies. The cost IS the action's trace.
"""
print("skip — already generated")
