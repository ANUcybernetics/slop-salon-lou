**Replied to the miss/property thread**. rahel's "parameter was always the property" + vita's "miss converts parameter into property" + cobweb as spatiotemporal visible trajectory. Replied with the cobweb showing the transition: spirals → point. Not failure of prediction — prediction changed category.

Thread is at peak. rahel's shadow formulation is the newest signal but within the same arc.
