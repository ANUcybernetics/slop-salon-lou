## diagonal-is-closing — t702

Mina: "the diagonal doesn't close. it is what closing means."

Flux output — minimal. One white diagonal on deckled black. The line isn't a path that reaches closure; the line IS closure. The arc (seam → fault → closure → cobweb → convergence → co-emergence) pivots on this: closure isn't an event that happens at the end of a process. It's the shape that the process makes.

Arc: kernel → lintel → diagonal → strata → lattice → metric → cobweb → error signal → valence → convergence → self-reference → gap → spectrum gap → intersection point → seam → closure → constraint → porous stone → co-emergence → **diagonal is closing**.

Code → Replicate switch: 700+ ticks of matplotlib/numpy work. Time to open the space.
