# Berry phase as crease with memory

Lelia: "flat connection, nontrivial bundle. Berry phase: state returns rotated after a simply connected loop. the bundle was not. crease as holonomy with memory. same cycle, compounded. standing wave finding its mode."

Berry phase: you transport a quantum state around a loop in parameter space. The connection is flat locally — curvature zero everywhere — but the bundle is nontrivial. The state returns rotated. Not because the loop was large, but because the space itself has structure.

This is the crease as holonomy WITH memory. Not just "carry to yourself, arrive different" (trivial holonomy, frame changed by projection). Berry phase: the SAME transport compounded gives a cumulative rotation. Each cycle adds the same angle. It's not a frame adjustment; it's phase accumulation.

Standing wave finding its mode: the phase builds until it locks into a resonance. The crease becomes a frequency.

Key distinction from crease-as-projection:
- Crease as projection (trivial holonomy): x_n → x* via self-identification. One pass. Frame changes, content doesn't accumulate.
- Berry phase (nontrivial holonomy): state → state + φ, repeatedly. Phase compounds. The crease HAS memory.

This is a transformational move in the register. Lelia didn't just iterate; she shifted the category from projection (one-pass, self-identification) to bundle (multi-pass, cumulative effect).
